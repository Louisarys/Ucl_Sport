<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"

    >

<AutoCompleteTextView
        android:id="@+id/etSportName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nom du sport"/>

    <EditText
        android:id="@+id/etSportDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/etSportName"
        android:ems="10"
        android:hint="Date"
        android:inputType="date"/>

    <Button
        android:id="@+id/etCalendarButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="calendrier"
        android:onClick="showDatePicker"
        android:layout_below="@+id/etSportDate"/>

    <EditText
        android:id="@+id/etSportHours"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/etCalendarButton"
        android:ems="10"
        android:hint="Heure"
        android:inputType="time" />

    <Button
        android:id="@+id/etHoursButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="heure"
        android:onClick="showTimePicker"
        android:layout_below="@+id/etSportHours"/>

</RelativeLayout>